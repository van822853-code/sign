# Frontend calls the Cloudflare Worker API.
# Local dev can use `http://127.0.0.1:8787` and auto-fallback to nearby local ports.
# Optional override if you want to point to a different Worker during development.
VITE_EVENT_API_BASE="http://127.0.0.1:8787"
