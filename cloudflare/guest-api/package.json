{
  "name": "ensemble-guest-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler check",
    "types": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
